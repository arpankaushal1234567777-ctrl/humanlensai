services:
  - type: web
    name: humanlens-ml
    runtime: python
    plan: free
    buildCommand: pip install -r deploy_hf/requirements.txt
    startCommand: uvicorn deploy_hf.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
